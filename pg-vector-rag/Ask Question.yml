info:
  name: Ask Question
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8000/ask
  body:
    type: json
    data: |-
      {
        "question": "Explain the time complexity of graph algorithms"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​